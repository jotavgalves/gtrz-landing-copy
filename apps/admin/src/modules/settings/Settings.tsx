import { useEffect,useMemo,useState } from 'react';
import { api } from '../../lib/api';
import { Icon } from '../../components/Icons';
import { Empty,Notice,Panel } from '../../components/StateViews';

type SettingRow={key:string;value_json:string;updated_at:string};
const generalDefaults={siteName:'GTRZ Eventos',tagline:'Produção · cultura · experiência',contactEmail:'gtrzeventos@gmail.com',contactWhatsapp:'',defaultLocale:'pt-BR',homepageMode:'hub'};
const socialDefaults={instagram:'https://instagram.com/gtrzeventos',tiktok:'',youtube:'',whatsapp:'',x:''};
const chromeDefaults={
  metaTitlePt:'GTRZ Eventos',metaTitleEs:'GTRZ Eventos',
  metaDescriptionPt:'GTRZ Eventos — experiências culturais entre Venezuela e Brasil.',metaDescriptionEs:'GTRZ Eventos — experiencias culturales entre Venezuela y Brasil.',
  footerPt:'GTRZ Eventos · Venezuela → Brasil',footerEs:'GTRZ Eventos · Venezuela → Brasil',eventsLabelPt:'Eventos',eventsLabelEs:'Eventos',
  gateEyebrow:'Venezuela → Brasil',gateTitlePrefix:'COMO VOCÊ QUER',gateTitleHighlight:'ENTRAR?',
  gateBodyPt:'Escolha o idioma da experiência.',gateBodyEs:'Elige el idioma de la experiencia.',
  gateHintPt:'Você pode mudar o idioma novamente no menu do site.',gateHintEs:'Puedes cambiar el idioma nuevamente en el menú.',
  navPt:[['Sobre','#sobre'],['Ritmos','#ritmos'],['Diferenciais','#diferenciais'],['Quem faz','#quem'],['Trabalhe conosco','#trabalhe'],['Parcerias','#parcerias'],['Instagram','#instagram']],
  navEs:[['Sobre','#sobre'],['Ritmos','#ritmos'],['Diferenciales','#diferenciais'],['Quiénes somos','#quem'],['Trabaja con nosotros','#trabalhe'],['Alianzas','#parcerias'],['Instagram','#instagram']]
};

function safeParse(value:string,fallback:any={}){try{return JSON.parse(value)}catch{return fallback}}
function navText(value:any){return (Array.isArray(value)?value:[]).map((item:any)=>Array.isArray(item)?`${item[0]||''}|${item[1]||''}`:'').filter(Boolean).join('\n')}
function parseNav(value:string){return value.split('\n').map(line=>line.trim()).filter(Boolean).map(line=>{const [label,...href]=line.split('|');return [label?.trim()||'',href.join('|').trim()||'#']}).filter(([label])=>label)}

export function Settings(){
  const [rows,setRows]=useState<SettingRow[]>([]);const [general,setGeneral]=useState<any>(generalDefaults);const [social,setSocial]=useState<any>(socialDefaults);const [chrome,setChrome]=useState<any>(chromeDefaults);const [navPt,setNavPt]=useState(navText(chromeDefaults.navPt));const [navEs,setNavEs]=useState(navText(chromeDefaults.navEs));const [selected,setSelected]=useState<string>('');const [raw,setRaw]=useState('{}');const [newKey,setNewKey]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);
  const load=async()=>{const data=await api<any>('/api/admin/settings');const next=data.results||[];setRows(next);const g=next.find((x:SettingRow)=>x.key==='general');const s=next.find((x:SettingRow)=>x.key==='social');const ch=next.find((x:SettingRow)=>x.key==='chrome');const nextGeneral={...generalDefaults,...(g?safeParse(g.value_json):{})};const nextSocial={...socialDefaults,...(s?safeParse(s.value_json):{})};const nextChrome={...chromeDefaults,...(ch?safeParse(ch.value_json):{})};setGeneral(nextGeneral);setSocial(nextSocial);setChrome(nextChrome);setNavPt(navText(nextChrome.navPt));setNavEs(navText(nextChrome.navEs));if(selected){const current=next.find((x:SettingRow)=>x.key===selected);if(current)setRaw(JSON.stringify(safeParse(current.value_json),null,2))}};
  useEffect(()=>{load().catch(e=>setMessage(e.message));},[]);
  const saveKey=async(key:string,value:any)=>{setBusy(true);setMessage('');try{await api(`/api/admin/settings/${encodeURIComponent(key)}`,{method:'PUT',body:JSON.stringify(value)});setMessage(`Configuração “${key}” salva.`);await load();}catch(e:any){setMessage(e.message)}finally{setBusy(false)}};
  const saveChrome=()=>saveKey('chrome',{...chrome,navPt:parseNav(navPt),navEs:parseNav(navEs)});
  const openAdvanced=(row:SettingRow)=>{setSelected(row.key);setRaw(JSON.stringify(safeParse(row.value_json),null,2));};
  const saveRaw=async()=>{try{await saveKey(selected,JSON.parse(raw));}catch{setMessage('JSON inválido. Corrija o conteúdo antes de salvar.')}};
  const createSetting=async()=>{const key=newKey.trim().toLowerCase().replace(/[^a-z0-9_-]+/g,'_');if(!key)return;await saveKey(key,{});setNewKey('');setSelected(key);setRaw('{}')};
  const advancedRows=useMemo(()=>rows.filter(r=>!['general','social','event_popup','chrome'].includes(r.key)),[rows]);
  return <div className="module-stack">
    <Panel title="Site e identidade" eyebrow="Configurações" description="Dados institucionais e canais globais utilizados pelo ecossistema GTRZ.">
      <div className="settings-grid">
        <section className="settings-card"><div className="settings-card-head"><div><span>GERAL</span><h3>Identidade institucional</h3></div><Icon name="globe"/></div><div className="form-grid"><label><span>Nome da marca</span><input value={general.siteName||''} onChange={e=>setGeneral((v:any)=>({...v,siteName:e.target.value}))}/></label><label><span>Idioma padrão</span><select value={general.defaultLocale||'pt-BR'} onChange={e=>setGeneral((v:any)=>({...v,defaultLocale:e.target.value}))}><option value="pt-BR">Português</option><option value="es">Español</option></select></label><label className="wide"><span>Página inicial publicada</span><select value={general.homepageMode||'hub'} onChange={e=>setGeneral((v:any)=>({...v,homepageMode:e.target.value}))}><option value="hub">Hub temporário de eventos</option><option value="full">Site institucional completo</option></select><small>Enquanto estiver em “Hub temporário”, a raiz gtrz.com.br mostra somente a agenda de eventos. As páginas individuais continuam públicas.</small></label><label className="wide"><span>Assinatura / tagline</span><input value={general.tagline||''} onChange={e=>setGeneral((v:any)=>({...v,tagline:e.target.value}))}/></label><label><span>E-mail de contato</span><input type="email" value={general.contactEmail||''} onChange={e=>setGeneral((v:any)=>({...v,contactEmail:e.target.value}))}/></label><label><span>WhatsApp institucional</span><input value={general.contactWhatsapp||''} onChange={e=>setGeneral((v:any)=>({...v,contactWhatsapp:e.target.value}))}/></label></div><button className="primary" disabled={busy} onClick={()=>saveKey('general',general)}><Icon name="save" size={16}/> Salvar geral</button></section>
        <section className="settings-card"><div className="settings-card-head"><div><span>SOCIAL</span><h3>Canais oficiais</h3></div><Icon name="link"/></div><div className="form-grid"><label className="wide"><span>Instagram</span><input value={social.instagram||''} onChange={e=>setSocial((v:any)=>({...v,instagram:e.target.value}))}/></label><label><span>TikTok</span><input value={social.tiktok||''} onChange={e=>setSocial((v:any)=>({...v,tiktok:e.target.value}))}/></label><label><span>YouTube</span><input value={social.youtube||''} onChange={e=>setSocial((v:any)=>({...v,youtube:e.target.value}))}/></label><label><span>WhatsApp</span><input value={social.whatsapp||''} onChange={e=>setSocial((v:any)=>({...v,whatsapp:e.target.value}))}/></label><label><span>X / Twitter</span><input value={social.x||''} onChange={e=>setSocial((v:any)=>({...v,x:e.target.value}))}/></label></div><button className="primary" disabled={busy} onClick={()=>saveKey('social',social)}><Icon name="save" size={16}/> Salvar redes</button></section>
        <section className="settings-card interface-card"><div className="settings-card-head"><div><span>INTERFACE PÚBLICA</span><h3>Header, idioma, SEO e rodapé</h3></div><Icon name="content"/></div><div className="form-grid"><label><span>Título SEO · PT</span><input value={chrome.metaTitlePt||''} onChange={e=>setChrome((v:any)=>({...v,metaTitlePt:e.target.value}))}/></label><label><span>Título SEO · ES</span><input value={chrome.metaTitleEs||''} onChange={e=>setChrome((v:any)=>({...v,metaTitleEs:e.target.value}))}/></label><label><span>Descrição SEO · PT</span><textarea value={chrome.metaDescriptionPt||''} onChange={e=>setChrome((v:any)=>({...v,metaDescriptionPt:e.target.value}))}/></label><label><span>Descrição SEO · ES</span><textarea value={chrome.metaDescriptionEs||''} onChange={e=>setChrome((v:any)=>({...v,metaDescriptionEs:e.target.value}))}/></label><label><span>Botão eventos · PT</span><input value={chrome.eventsLabelPt||''} onChange={e=>setChrome((v:any)=>({...v,eventsLabelPt:e.target.value}))}/></label><label><span>Botão eventos · ES</span><input value={chrome.eventsLabelEs||''} onChange={e=>setChrome((v:any)=>({...v,eventsLabelEs:e.target.value}))}/></label><label><span>Rodapé · PT</span><input value={chrome.footerPt||''} onChange={e=>setChrome((v:any)=>({...v,footerPt:e.target.value}))}/></label><label><span>Rodapé · ES</span><input value={chrome.footerEs||''} onChange={e=>setChrome((v:any)=>({...v,footerEs:e.target.value}))}/></label></div><div className="settings-subsection"><small>PORTA DE IDIOMA</small><div className="form-grid"><label><span>Linha de apoio</span><input value={chrome.gateEyebrow||''} onChange={e=>setChrome((v:any)=>({...v,gateEyebrow:e.target.value}))}/></label><label><span>Título</span><input value={chrome.gateTitlePrefix||''} onChange={e=>setChrome((v:any)=>({...v,gateTitlePrefix:e.target.value}))}/></label><label><span>Destaque vermelho</span><input value={chrome.gateTitleHighlight||''} onChange={e=>setChrome((v:any)=>({...v,gateTitleHighlight:e.target.value}))}/></label><label><span>Texto · PT</span><input value={chrome.gateBodyPt||''} onChange={e=>setChrome((v:any)=>({...v,gateBodyPt:e.target.value}))}/></label><label><span>Texto · ES</span><input value={chrome.gateBodyEs||''} onChange={e=>setChrome((v:any)=>({...v,gateBodyEs:e.target.value}))}/></label><label><span>Dica · PT</span><input value={chrome.gateHintPt||''} onChange={e=>setChrome((v:any)=>({...v,gateHintPt:e.target.value}))}/></label><label><span>Dica · ES</span><input value={chrome.gateHintEs||''} onChange={e=>setChrome((v:any)=>({...v,gateHintEs:e.target.value}))}/></label></div></div><div className="settings-subsection"><small>NAVEGAÇÃO</small><div className="form-grid"><label><span>Menu PT</span><textarea className="code-editor compact" value={navPt} onChange={e=>setNavPt(e.target.value)}/><small>Uma linha por item: Nome|#ancora</small></label><label><span>Menu ES</span><textarea className="code-editor compact" value={navEs} onChange={e=>setNavEs(e.target.value)}/><small>Una línea por elemento: Nombre|#ancla</small></label></div></div><button className="primary" disabled={busy} onClick={saveChrome}><Icon name="save" size={16}/> Salvar interface pública</button></section>
      </div>{message&&<Notice tone={message.toLowerCase().includes('inválido')||message.toLowerCase().includes('error')?'error':'success'}>{message}</Notice>}
    </Panel>

    <Panel title="Configurações avançadas" eyebrow="Chaves do site" description="Tudo que existe em site_settings pode ser inspecionado e editado. As chaves do popup ficam no módulo Marketing.">
      <div className="advanced-settings">
        <aside className="settings-key-list"><div className="settings-new-key"><input value={newKey} onChange={e=>setNewKey(e.target.value)} placeholder="nova_chave"/><button className="icon-button" onClick={createSetting} title="Criar chave"><Icon name="plus"/></button></div>{advancedRows.length?advancedRows.map(row=><button key={row.key} className={selected===row.key?'active':''} onClick={()=>openAdvanced(row)}><strong>{row.key}</strong><small>{new Date(row.updated_at).toLocaleString('pt-BR')}</small></button>):<Empty icon="settings">Nenhuma chave adicional.</Empty>}</aside>
        <div className="settings-json-editor">{selected?<><div className="editor-section-head"><div><small>CHAVE</small><h3>{selected}</h3></div><button className="primary" onClick={saveRaw} disabled={busy}><Icon name="save" size={16}/> Salvar JSON</button></div><textarea className="code-editor" spellCheck={false} value={raw} onChange={e=>setRaw(e.target.value)}/><p className="hint">Objeto JSON persistido no D1. Use este editor para opções que ainda não possuem formulário visual dedicado.</p></>:<div className="settings-empty"><Icon name="settings" size={28}/><strong>Selecione uma chave</strong><span>Você verá o JSON completo e poderá editar qualquer propriedade.</span></div>}</div>
      </div>
    </Panel>
  </div>;
}
